/* === UEMA — dashboard-data.js === */
/* Gerado automaticamente — dados serão preenchidos na atualização */

const UEMA_ALL_DATA = [];
const UEMA_RESULTS_DATA = [];

window.INSTITUTION_DATASETS = window.INSTITUTION_DATASETS || {};
window.INSTITUTION_DATASETS['uema'] = {
  institutionName: 'UEMA',
  slug: 'uema',
  allData: UEMA_ALL_DATA,
  resultsData: UEMA_RESULTS_DATA,
};
