Fonte única do painel do coordenador (shared.js + shared.css).

Editar apenas aqui. Depois rodar na raiz do repositório:

  python3 scripts/sync-panel-core.py

Isso replica para a raiz e para todas as pastas de IES que já possuem shared.js.
