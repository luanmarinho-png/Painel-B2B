/* === UNOCHAPECÓ — dashboard-data.js === */
/* Gerado automaticamente — dados serão preenchidos na atualização */

const UNOCHAPECO_ALL_DATA = [];
const UNOCHAPECO_RESULTS_DATA = [];

window.INSTITUTION_DATASETS = window.INSTITUTION_DATASETS || {};
window.INSTITUTION_DATASETS['unochapeco'] = {
  institutionName: 'UNOCHAPECÓ',
  slug: 'unochapeco',
  allData: UNOCHAPECO_ALL_DATA,
  resultsData: UNOCHAPECO_RESULTS_DATA,
};
