/* === UNICERRADO — dashboard-data.js === */
/* Gerado automaticamente — dados serão preenchidos na atualização */

const UNICERRADO_ALL_DATA = [];
const UNICERRADO_RESULTS_DATA = [];

window.INSTITUTION_DATASETS = window.INSTITUTION_DATASETS || {};
window.INSTITUTION_DATASETS['unicerrado'] = {
  institutionName: 'UNICERRADO',
  slug: 'unicerrado',
  allData: UNICERRADO_ALL_DATA,
  resultsData: UNICERRADO_RESULTS_DATA,
};
