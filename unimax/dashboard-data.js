/* === UNIMAX — dashboard-data.js === */
/* Gerado automaticamente — dados serão preenchidos na atualização */

const UNIMAX_ALL_DATA = [];
const UNIMAX_RESULTS_DATA = [];

window.INSTITUTION_DATASETS = window.INSTITUTION_DATASETS || {};
window.INSTITUTION_DATASETS['unimax'] = {
  institutionName: 'UNIMAX',
  slug: 'unimax',
  allData: UNIMAX_ALL_DATA,
  resultsData: UNIMAX_RESULTS_DATA,
};
